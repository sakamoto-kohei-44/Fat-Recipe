<div class="h-screen flex flex-col items-center justify-center px-8 bg-green-100">
  <%= form_with(url: "/recipes", method: :get, local: true) do |form| %>
    <div class="bg-white mt-6 rounded-lg shadow-md w-full max-w-md px-10 py-8">
      <h1 class="text-2xl font-bold mb-6 text-gray-800">レシピ検索</h1>
        <div class="flex flex-col mb-6">
          <%= form.label :calories, "必要な1日カロリー(kcal)", class: "mb-2 text-lg text-gray-700" %>
          <%= form.number_field :calories, in: 1000..4000, step: 100,
                                class: "p-4 mt-2 border-2 border-gray-300 rounded-lg shadow-sm focus:border-indigo-500 focus:ring focus:ring-indigo-200 focus:ring-opacity-50 w-full" %>
        </div>
        <%= form.submit "レシピ検索", class: "w-full bg-indigo-500 hover:bg-indigo-600 text-white py-3 px-8 rounded-lg shadow-lg transition ease-in-out duration-300" %>
      </div>
    </div>
  <% end %>
  <p class="mt-8 text-lg text-gray-700 text-center max-w-md">
    あなたの必要カロリー量を入力してレシピを検索できます。
  </p>
</div>
